{
  "name": "ev3nts-backend",
  "version": "1.0.0",
  "description": "web3 events smart contract",
  "main": "index.js",
  "scripts": {
    "script": "node scripts/run.js",
    "deploy": "npx hardhat run scripts/deploy.js --network mumbai",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "andrew henry",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.10.2"
  },
  "dependencies": {
    "dotenv": "^16.0.1"
  }
}
